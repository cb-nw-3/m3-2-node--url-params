<div class='main'>
    <ol>
        <% books.forEach(book => { %>
        <li> <%= book.title %>, By: <%= book.author %>   <img src='<%= book.imgUrl %>'/>' </li>
        <% }); %>
        </ol>
    </div>