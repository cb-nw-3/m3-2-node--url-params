<div class='main'>
    <ol>
        <% books.forEach(book => { %>
        <li>ID# <%= book.id %>. <%= book.title %> <%= book.type %> <%= book.author %> <%= book.description %> <%= book.imgUrl %> </li>
        <% }); %>
        </ol>
    </div>