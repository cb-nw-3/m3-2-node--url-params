<%- include('../partials/header.ejs', {}) %>
<ol>
    <% books.forEach(book => { %>
        <li class=book>  
            <a href=<%-`http://localhost:8000/books/${book.id}`%>><%- `${book.id}. ${book.title} by ${book.author}` %></a>
        </li>
    <% }); %>
</ol>
<%- include('../partials/footer.ejs', {}) %>