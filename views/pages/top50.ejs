<%- include('../partials/header') %>
<ul class='list'>
    <% top50.forEach((item) => { %>
        <%- include('../partials/song', {item: item}) %>
    <% }) %>
</ul>
<%- include('../partials/footer') %>